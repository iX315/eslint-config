// Invalid TypeScript code with style violations
// - Has semicolons
// - Has double quotes
// - Has 4 space indent

const data: any = { foo: "bar" };

function processData(input: any): string {
    return input.toString();
}

export { data, processData }
