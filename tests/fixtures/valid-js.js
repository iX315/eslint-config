// Valid JS code following our style rules
// - No semicolons
// - Single quotes
// - 2 space indent
// - No trailing spaces
// - Max 120 chars per line

const message = 'hello world'

function greet(name) {
  console.log(`Hello, ${name}`)
  return true
}

const obj = { foo: 'bar', baz: 42 }

export { greet, obj }
